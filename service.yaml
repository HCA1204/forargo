apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  labels:
    app: nginx
spec:
  ports:
  - port: 80
    protocol: TCP
  selector:
    app: nginx
---
apiVersion: v1
kind: Service
metadata:
  name: helloworld2-svc
  labels:
    app: helloworld2
spec:
  ports:
    - port: 3080
  selector:
    app: helloworld2
